# 数据质量与长度捷径检查

> 生成日期：2026-07-13  
> 数据 SHA-256：`5615a96a7894caed5d14463c77167af8098bdc1e1ebf32a33a89a12c3c5cf6e6`

## 数据完整性

- 原始记录 46,535 条，三个字段均无缺失值。
- 功率字符串中没有无法解析或非有限数值 token。
- 过滤标签 5 和长度小于 18 的序列后，有效三分类记录为 35,099 条。
- 原始数据有 9,106 条恒定序列；过滤后只剩 4 条恒定序列。
- 原始数据有 269 个重复序列组和 1 个标签冲突组；过滤后的有效数据没有重复序列或冲突标签。
- 当前最长有效序列为 816，因此 `max_length=1024` 时没有序列被截断。

## 标签 5 的过滤依据

标签 5 共 11,435 条，其中 10,613 条短于 18 个时间点，序列长度中位数为 1，且 9,101 条为
恒定序列。数据特征支持将其视为不完整记录并从故障三分类中分离，但最终论文仍需补充数据提供者
对标签 5 产生机制的说明。

## 序列长度捷径

使用单一“序列长度”特征训练深度为 3 的决策树，在固定测试集上得到：

| 指标 | 结果 |
|---|---:|
| Accuracy | 0.455983 |
| Balanced Accuracy | 0.424138 |
| Macro-F1 | 0.357617 |
| Weighted-F1 | 0.382655 |

该结果明显高于三分类多数类约 36.94% 的准确率，说明标签和序列长度存在一定关联。正式模型必须
使用 mask 排除 padding，并报告长度分组结果，避免把长度差异误解为功率趋势识别能力。

## 划分限制

`charging_station_id` 在当前文件中每行唯一，没有可重复的设备或站点分组字段；CSV 也没有时间戳。
因此当前只能进行分层随机划分，不能验证跨设备或跨时间泛化。论文中应明确这一限制；若能取得
设备 ID 和采集时间，应增加 group split 或 time split 实验。

完整机器可读结果和典型曲线由以下命令生成：

```bash
uv run moment-analyze-data --config configs/moment.yaml
```
