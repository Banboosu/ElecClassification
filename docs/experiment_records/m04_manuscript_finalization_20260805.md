# M04 论文与投稿材料闭环（2026-08-05）

## 1. 完成结论

M04 最终状态为 **PASSED**。本次只使用已有正式运行、派生分析和论文图表完成文稿闭环，没有启动
新训练，也没有修改或替换已投入使用的旧 TCN 代码与模型。

正文数值、主图表、模型名称、单位和结论边界均已统一。自动审计从正式 JSON/CSV 产物核对了
176 个正文数值单元，233 项总检查全部通过；表 1–12 和图 1–7 连续编号，7 幅正文图均同时提供
PNG 与 SVG，且 PNG 分辨率满足当前成稿要求。

## 2. 文稿闭环内容

- 主结果表补齐 MOMENT 线性探测和最后两层微调的 Balanced Accuracy，分别为
  **77.31% ± 0.93%** 和 **82.05% ± 1.31%**；其余主表数值逐项复核。
- 表 5 和图 1 给出五档少标签学习曲线；表 8 给出完整标签 TCN 与完全微调 MOMENT 的五种子
  行归一化混淆矩阵。
- 图 2 保留 M02 的六类固定规则错误曲线；图 3–5 完成安全阈值和电池专用分类对比；图 6–7
  给出无监督检索和零样本插补边界。
- 中英文摘要、正文讨论、结论和局限性使用相同主张边界：MOMENT 的可重复正面结果限定为当前
  管线下的低标签效率与稳定性；标签充足且关注已测训练侧资源时，当前证据支持优先 TCN。
- 额外强基线、输入退化、概率校准、统一在线推理基准和生产兼容离线回放均归入“未来工作”，
  不再作为当前离线结论成立的前置条件。
- 稿件中的 `【待补】` 仅保留作者、单位、基金、CRediT、利益冲突和致谢等投稿元数据。

## 3. 图表样式与命名规则

新增 `src/tcn_moment/paper_style.py` 作为论文图公共样式入口，统一使用 DejaVu Sans、浅色网格、
300 DPI 默认导出和色盲友好配色。核心模型颜色固定为：TCN 蓝色、冻结 MOMENT + RBF-SVM
橙色、完全微调 MOMENT 紫色、随机森林绿色、逻辑回归灰色。

图 3–7 已由既有汇总产物重新导出 PNG/SVG。图 1 保留已有少标签成稿，图 2 保留 M02 固定规则
错误图；二者已经满足清晰度、模型区分和矢量导出要求。本次没有覆盖进入 M04 前已经存在的
少标签 SVG 本地修改。

模型名统一采用“1D-CNN（逐序列 z-score）”“TCN（原始尺度/逐序列 z-score/逐序列
min-max）”“MOMENT 线性探测/最后两层微调/冻结表征 + RBF-SVM/完全微调”。分类聚合指标以
百分数、两位小数和“均值 ± 样本标准差”报告。

## 4. 自动审计范围

机器审计入口为 `scripts/audit_m04_manuscript.py`，覆盖：

- 表 2 的 11 个模型与 Accuracy、Balanced Accuracy、Macro-F1；
- 表 5 的五档少标签结果和方法差值；
- 表 6 的 M01 预训练归因结果与配对区间；
- 表 7–8 的 M02 长度分层和混淆矩阵；
- 表 9 的电池安全运行点；
- 表 10–11 的检索与插补结果；
- 表号、图号、PNG/SVG 配对、PNG 分辨率、模型别名、实验占位符和未来工作边界。

最终结果为 `status=passed`、`numeric_cells=176`、`passed_check_count=233`、`failures=[]`。

## 5. 验证结果

- 远程正式 M04 审计：退出码 0，**PASSED**。
- 正式 `tests/` 测试目录：**56 passed in 6.51s**。
- M04 新增及修改的 7 个 Python 文件：Ruff **All checks passed**。
- Python 字节码编译和目标文件 `git diff --check`：通过。
- 远端工作区根目录另有历史 `src_sync_tmp` 备份测试副本；为避免同名测试模块收集冲突，回归测试
  明确限定在正式 `tests/` 目录，没有删除或修改该备份目录。

## 6. 产物与复现

- 审计结果目录：`artifacts/analysis/m04_manuscript_audit_20260805/`。
- 机器可读结果：`audit.json`。
- 人类可读摘要：`summary.md`。
- 远程运行输出：`run.log`。
- 正文图校验和：`figure_sha256.txt`。
- 本地归档：`artifacts/imports/m04_manuscript_finalization_20260805.tar.gz`，共 26 个归档条目。
- 归档 SHA-256：
  `a607f17799b3e629ae0661f41ceb2db772a94858be457fcf85326dab3adecc98`。

归档包含审计结果、最终正文图、文稿快照和复现脚本，不包含模型权重或逐样本内部数据。

## 7. 对论文结论的影响

M04 不改变既有实验结果，而是完成数字可追溯、图表齐备和主张边界一致性收口。当前稿件可以按
既有离线证据进入格式调整与投稿元数据补充阶段；未来扩展若执行，应独立报告，不能回写为当前
结论成立所必需的实验。
