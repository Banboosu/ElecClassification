# 学生论文最小实验闭环 TODO

> 建立日期：2026-08-05  
> 当前执行依据：本文件  
> 扩展实验备忘：[paper_experiment_backlog_20260805.md](paper_experiment_backlog_20260805.md)

## 1. 论文范围

本文以完成一篇结构完整、结论谨慎、实验可复现的学生论文或普通期刊论文为目标，不追求一次性
完成所有基础模型、迁移学习、生产回放和安全部署实验。

当前最稳妥的核心结论是：

> 在当前真实内部充电功率三分类任务中，预训练 MOMENT 表征配合 RBF-SVM 在 1%–10% 标签条件下
> 比从头训练 TCN 获得更高的 Macro-F1 和更低的跨随机种子波动；标签充足时，TCN 以更低训练成本
> 获得接近或更高的分类性能。

论文不声称：

- MOMENT 全面优于 TCN；
- 当前结果达到该领域最优；
- 单一离线指标可以直接推动线上模型替换；
- 模型已经能够执行无漏检的安全报警或早期预警；
- 现有结果证明所有优势都由预训练单独造成。

## 2. 已有证据，不需要重做

- [x] 数据为真实内部运行资源，标签 0/1/2/5 沿用现行业务口径。
- [x] 旧 TCN 代码与模型已经投入实际使用。
- [x] 固定分层划分、样本清单、padding mask 和统一指标已经完成。
- [x] 多数类、逻辑回归、随机森林、1D-CNN、TCN 和 MOMENT 主对比已经完成。
- [x] 完整标签结果已经完成五随机种子汇总。
- [x] 1%、5%、10%、20%、40% 少标签学习曲线已经完成五随机种子汇总。
- [x] MOMENT 线性探测、部分微调、RBF-SVM 和完全微调对比已经完成。
- [x] 电池异常 Recall、FPR、F2、PR-AUC 和验证集阈值复评已经完成。
- [x] 无监督检索和零样本插补已经完成，可作为正负迁移边界结果。
- [x] 训练时间、峰值显存和参数量已经记录。
- [x] M01 同架构预训练—随机初始化归因消融已经完成，三个低标签比例的配对区间均高于 0。

## 3. 投稿前真正需要完成的工作

### M01 预训练归因简化消融

目的：判断低标签收益是否至少部分来自 MOMENT 预训练权重。

- [x] 条件 A：预训练 MOMENT-1-large，冻结 encoder，使用当前 RBF-SVM。
- [x] 条件 B：相同架构随机初始化，冻结 encoder，使用完全相同的 RBF-SVM。
- [x] 只运行 1%、5%、10% 标签比例和 seeds 42–46。
- [x] 两个条件使用相同样本 ID、预处理、池化方式、特征维度和 SVM 参数搜索范围。
- [x] 报告 Macro-F1 均值、标准差、逐 seed 配对差和 95% 区间。

结果：预训练相对随机初始化在 1%、5%、10% 标签下分别提高 28.60、22.58、22.01 个
Macro-F1 百分点，95% 配对区间为 [22.24, 34.97]、[20.89, 24.28]、[20.48, 23.53]
个百分点，15 个逐 seed 差值均为正。完整记录见
[M01 预训练归因简化消融](experiment_records/m01_pretraining_ablation_20260805.md)。

不要求增加时间打乱、有效秩、类内距离、密集标签比例或 AULC。若该实验暂时无法完成，论文把
“预训练提高标签效率”降级为“预训练 MOMENT + RBF-SVM 管线在低标签条件下优于当前 TCN”。

### M02 轻量错误分析

- [ ] 为 TCN、完全微调 MOMENT 和低标签 MOMENT-SVM 保存逐样本预测及类别分数。
- [ ] 汇总每类主要错误方向，重点报告电池异常漏检和高置信误报。
- [ ] 绘制少量固定规则选出的典型错误曲线，不按视觉效果挑选样本。
- [ ] 按有效序列长度分为 3–5 个区间，报告各区间 Macro-F1。
- [ ] 用一小段文字归纳模型共同错误、仅 TCN 错误和仅 MOMENT 错误的曲线特征。

可选：如果现有推理脚本很容易支持，只增加一个噪声条件或一个 50% 输入前缀条件。高斯噪声、
随机缺失、连续缺失、下采样、幅值漂移和多级前缀的完整网格不属于当前必做范围。

### M03 实验协议最终检查

- [ ] 核对正式表格中的所有运行均使用固定 split manifest 和相同指标实现。
- [ ] 检查 CNN 的五随机种子 AMP 运行是否完整、无 NaN/Inf、无自动协议变化。
- [ ] 若 CNN 结果稳定，直接在论文中记录 AMP 配置；只有发现数值异常时才重跑 FP32。
- [ ] 统一结果表中的百分数、小数位、均值 ± 标准差和模型名称。
- [ ] 明确计算成本结论仅覆盖训练时间、显存和参数量，不直接外推为线上推理速度。

### M04 论文与投稿材料

- [ ] 删除或改写正文中“必须完成 E02–E04、生产回放后才能成立”等过强待办表述。
- [ ] 将未执行实验统一放入“未来工作”，不再列作当前硬缺口。
- [ ] 核对正文数字与综合实验报告、图表和原始产物一致。
- [ ] 完成主结果表、少标签学习曲线、混淆矩阵、安全阈值图和错误案例图。
- [ ] 统一图号、表号、字体、颜色、单位和中英文模型名称。
- [ ] 确定目标学校/期刊格式和参考文献样式。
- [ ] 补充作者、单位、基金、作者贡献、利益冲突和数据可用性声明。
- [ ] 完成中文摘要、英文摘要、结论和局限性的最终互相核对。

## 4. 有余力再做，不属于当前硬要求

### O01 单个强基线

若准备正式期刊投稿且时间允许，只补 MiniROCKET 或 ROCKET 中的一种，并使用相同 1%、5%、10%
标签比例。无需同时实现 TS2Vec、InceptionTime、Hydra 和其他基础模型。

### O02 简化推理效率

只有论文继续强调“部署效率”时才做：在同一硬件上测试 batch 1 和 batch 32 的 P50/P95 推理延迟、
吞吐、峰值显存和模型大小。否则正文只保留已经有证据的“训练侧效果—成本权衡”。

### O03 简单人工复盘

如果能够方便地邀请了解业务的人员，可共同查看少量电池漏检和高置信误报；不要求组织 2–3 人
盲法检索评价、相关性问卷或复杂一致率统计。

## 5. 当前明确不做，统一列为未来工作

- [x] 不新增 0.5%、2%、15%、30%、60%、80% 等密集标签比例。
- [x] 不要求完成 TS2Vec、InceptionTime、Hydra/MultiROCKET 等整套强基线。
- [x] 不完成生产系统兼容回放，也不改动或替换已部署旧 TCN。
- [x] 不组织无监督检索的多人盲法人工相关性评价。
- [x] 不进行 LoRA、最后 8/12 层等参数高效微调网格。
- [x] 不进行目标域自监督重构适配。
- [x] 不训练完整的前缀早期检测模型组。
- [x] 不完成全套概率校准、选择性风险和能耗测量。
- [x] 不建设一条命令生成所有论文产物的完整自动化系统。

这些项目不属于“实验失败”或“论文缺陷”，只是在当前论文范围之外。

## 6. 推荐执行顺序

1. 先完成 M03，锁定已有结果并确认哪些运行可以直接进入论文。
2. M01 已完成：seed 42 冒烟通过后已扩展至 seeds 43–46，并完成配对统计。
3. 使用现有 checkpoint 完成 M02，尽量避免重新训练。
4. 集中完成 M04，不再新增研究方向。
5. 只有前四步完成且仍有时间时，选择 O01 或 O02 中的一项，不同时扩展两项。

## 7. 最小验收标准

满足以下条件即可结束补实验阶段：

- [x] 低标签核心结论有五随机种子结果，并明确它是“管线优势”还是“预训练归因结果”。
- [ ] 主表所有模型的数据划分、指标和结果来源可追踪。
- [ ] 至少包含逐类别结果、混淆矩阵、典型错误曲线和长度分层分析。
- [ ] 训练侧效果—成本结论与已有时间、显存、参数量记录一致。
- [ ] 所有未完成扩展实验均放入未来工作，没有继续出现在投稿硬缺口中。
- [ ] 摘要、正文、结论和局限性使用相同的主张边界。

## 8. 简化实验记录模板

每次新增运行只需记录：

```text
实验名称与日期：
代码版本与配置：
数据哈希、split manifest、随机种子：
主要指标和结果目录：
异常或偏离：
对论文结论的影响：
```
