# 项目 TODO

> 更新日期：2026-07-13  
> 原则：模型训练统一在远程 CUDA 环境完成；本地只进行代码检查、数据检查和小规模单元测试。  
> 当前阶段：统一数据、划分和指标已经完成，下一步先保证远程实验可靠，再做模型优化。

## 已完成

- [x] 标签 5 与短序列采用统一过滤规则，只保留标签 0、1、2。
- [x] TCN 和 MOMENT 共用 `configs/moment.yaml` 中的数据配置。
- [x] 使用固定随机种子的分层 train/validation/test 划分，比例为 70%/10%/20%。
- [x] 当前划分规模为训练 24,569、验证 3,510、测试 7,020。
- [x] 测试集不再参与逐 epoch 调参，仅用于最佳模型的最终评价。
- [x] 两种模型统一输出 Accuracy、Balanced Accuracy、Macro/Weighted Precision、Recall、F1。
- [x] 统一输出混淆矩阵和逐类别分类报告，未定义指标按 `zero_division=0` 处理。
- [x] TCN 和 MOMENT 均按验证集 Macro-F1 保存最佳权重。
- [x] 初始实验结果已归档至 `docs/experiment_records/initial_baseline_results.md`。

## P0：远程正式训练前必须完成

### 1. 防止实验结果相互覆盖

- [ ] 为每次运行增加唯一的 `run_name`，建议格式为“模型_预处理_随机种子_时间”。
- [ ] 输出到 `artifacts/<model>/<run_name>/`，不再固定覆盖 `artifacts/moment/metrics.json`。
- [ ] 每个运行目录保存实际生效的配置副本。
- [ ] 自动记录开始时间、结束时间、Git commit、Python/PyTorch/CUDA/GPU 信息。
- [ ] 保存运行状态：`running`、`completed`、`interrupted` 或 `failed`。

验收标准：连续运行两个配置后生成两个独立目录，任一目录都能单独还原实验条件。

### 2. 固化数据划分

- [ ] 数据加载时保留 `charging_station_id` 或原始行号。
- [ ] 第一次划分后保存 train/validation/test 的样本 ID 清单。
- [ ] 后续 TCN、MOMENT 和传统基线都读取同一份划分清单。
- [ ] 记录数据文件哈希、过滤前后数量及各集合类别分布。
- [ ] 检查三个集合的样本 ID 不相交，并在代码中加入断言。

验收标准：即使调整归一化或模型代码，同一实验协议中的样本归属仍完全一致。

### 3. 正确处理变长序列和 padding

- [ ] `DatasetBundle` 返回每条序列的有效长度或布尔 mask。
- [ ] 查明当前 `momentfm` 版本接受 padding mask 的准确参数，并在 MOMENT 前向传播中传入。
- [ ] TCN 的全局池化改为 mask-aware pooling，避免将尾部填充 0 当作真实功率。
- [ ] 验证截断前归一化、填充后 mask 的处理顺序。
- [ ] 在指标文件中记录因 `min_length` 和 `max_length` 被过滤或截断的数量。

验收标准：同一条序列仅改变尾部 padding 长度时，模型有效特征基本不变。

### 4. 修正环境与文档不一致

- [ ] 统一 README、`pyproject.toml` 和 `uv.lock` 中的 PyTorch/MOMENT 版本说明。
- [ ] 修正 `train_moment.py` 中不存在的 `uv sync --extra moment` 提示。
- [ ] 在远程 CUDA 主机执行一次全新环境安装验证。
- [ ] 记录远程驱动、CUDA 和显卡型号。
- [ ] 增加 Linux CUDA 环境的完整启动命令。

验收标准：在空目录克隆项目并补充数据后，按照 README 命令可以直接开始训练。

### 5. 增强训练中断与恢复能力

- [ ] MOMENT 增加 `--resume`，恢复模型、优化器、epoch、历史指标和最佳分数。
- [ ] TCN 同样保存 latest checkpoint 并支持 `--resume`。
- [ ] TCN 捕获 `KeyboardInterrupt`，确保中断后仍保存已完成 epoch 的结果。
- [ ] 保存随机数状态，尽可能保证恢复训练连续性。
- [ ] 写入临时文件后原子替换正式指标文件，避免中断产生损坏 JSON。

验收标准：训练中断后重新执行命令，可从上一个完整 epoch 继续，而不是从头开始。

## P1：核心实验与模型效果

### 6. 建立可复现的消融配置

- [ ] 不直接反复修改主配置，为每个实验建立独立 YAML。
- [ ] 归一化对比：`none`、`minmax`、`zscore`。
- [ ] 序列长度对比：256、512、816、1024。
- [ ] MOMENT 对比：冻结 backbone、解冻最后若干层、完全微调。
- [ ] 冻结 backbone 时测试分类头学习率 `1e-3`、`1e-4`、`1e-5`。
- [ ] 微调时为 backbone 和分类头设置不同学习率。
- [ ] 每个关键配置至少运行 5 个随机种子，并报告均值与标准差。

验收标准：实验配置可从文件名判断变量，除目标变量外其余条件保持一致。

### 7. 完善训练策略

- [ ] 增加基于验证集 Macro-F1 的 early stopping。
- [ ] 增加学习率调度器，并将每个 epoch 的学习率写入历史指标。
- [ ] 远程 CUDA 训练支持 AMP 混合精度，降低显存占用并提高速度。
- [ ] 支持梯度裁剪，记录是否发生 NaN/Inf。
- [ ] 输出模型总参数量、可训练参数量、单 epoch 时间和峰值显存。
- [ ] 根据远程显存测试合理的 batch size，不在本地做完整训练。

验收标准：长时间运行可自动停止并留下最佳模型、完整日志及资源消耗记录。

### 8. 增加正式基线

- [ ] 多数类基线。
- [ ] 统计特征 + Logistic Regression。
- [ ] 统计特征 + Random Forest 或 XGBoost/LightGBM（二选一即可）。
- [ ] 1D-CNN 基线。
- [ ] 当前统一协议下的 TCN。
- [ ] MOMENT 线性探测和微调结果。

统计特征至少包括长度、均值、标准差、最小值、最大值、分位数、首尾差、一阶差分统计和最大突变。

验收标准：所有模型使用同一划分清单和同一指标函数，结果能直接写入论文主对比表。

### 9. 数据质量与标签论证

- [ ] 在论文和 README 中说明标签 0、1、2、5 的业务含义和标注来源。
- [ ] 说明将标签 5 判为无效数据的依据，而不只是在代码中直接过滤。
- [ ] 输出每类序列长度、功率范围和典型曲线统计。
- [ ] 人工抽查异常值、解析失败值、恒定序列和疑似错误标签。
- [ ] 检查类别是否与序列长度强相关，避免模型只学习长度捷径。
- [ ] 评估按充电站、设备或时间划分的可行性，防止同源分布泄漏。

验收标准：能够在论文中回答“标签是否可靠、过滤是否合理、是否存在数据泄漏”。

## P2：论文结果自动化

### 10. 自动生成图表和表格

- [ ] 根据 `metrics.json` 生成训练/验证 loss 与 Macro-F1 曲线。
- [ ] 生成归一化混淆矩阵和原始计数混淆矩阵。
- [ ] 生成逐类别 Precision、Recall、F1 图。
- [ ] 聚合多随机种子结果，生成均值、标准差和箱线图。
- [ ] 生成模型参数量、训练时间、推理延迟和显存对比表。
- [ ] 导出适合论文使用的 PNG、PDF/SVG 和 CSV/Excel。
- [ ] 自动把最终运行结果追加到 Markdown 实验记录，而不是手工复制。

验收标准：执行一条报告命令即可从实验目录生成完整论文图表和汇总表。

### 11. 错误分析与鲁棒性实验

- [ ] 保存测试集每条样本的 ID、真实标签、预测标签和各类别概率。
- [ ] 汇总最典型的误分类样本并绘制原始曲线。
- [ ] 测试高斯噪声、随机缺失、序列截断和采样频率变化。
- [ ] 分析不同序列长度区间的 Macro-F1。
- [ ] 分析模型置信度，并考虑校准曲线或 Expected Calibration Error。

验收标准：论文中至少包含一节误差分析和一张鲁棒性结果表。

### 12. 实现论文创新模型

- [ ] 在可靠基线完成后再确定创新结构，避免同时改变过多因素。
- [ ] 优先方案：MOMENT 全局特征 + TCN 局部特征的双分支融合。
- [ ] 可加入功率均值、斜率、突变点等人工物理特征。
- [ ] 至少完成“仅 TCN、仅 MOMENT、直接融合、完整方案”的消融实验。
- [ ] 对融合权重或注意力结果进行可解释性展示。

验收标准：创新点带来的提升在相同协议和多个随机种子下仍然成立。

## P3：代码质量与最终演示

### 13. 测试和持续检查

- [ ] 为序列解析、归一化、截断、填充和过滤规则增加单元测试。
- [ ] 为固定划分、类别分布和无交叉泄漏增加测试。
- [ ] 为共享指标函数增加已知输入/输出测试。
- [ ] 为 TCN 和 MOMENT 各增加一个小张量前向测试。
- [ ] 配置 Ruff，并增加格式检查。
- [ ] 增加最小 CI，只运行轻量测试，不下载 MOMENT 权重、不训练模型。

### 14. 推理与演示

- [ ] 增加统一的 `predict` 命令，加载模型、标签编码器和训练配置。
- [ ] 输入一条原始功率序列，输出预测类别、置信度和基础统计信息。
- [ ] 对非法输入、过短序列和空序列给出清晰错误信息。
- [ ] 增加批量 CSV 推理和结果导出。
- [ ] 最后再制作 Streamlit 或简单 Web 演示页面。
- [ ] 测量单样本和批量推理延迟。

## 建议执行顺序

1. 完成 P0 的运行目录、划分清单、padding mask、环境和断点恢复。
2. 在远程运行统一 TCN 与 MOMENT 基线，确认代码和显存情况。
3. 完成 normalization、序列长度和冻结策略消融。
4. 补充传统机器学习与 1D-CNN 基线。
5. 自动生成论文图表，并进行错误分析。
6. 基于可靠结果实现双分支融合创新模型。
7. 最后完成测试、推理命令和演示页面。

## 远程运行注意事项

- `data/raw/` 和 `artifacts/` 当前被 Git 忽略，需要单独同步数据并下载实验结果。
- 正式实验前先运行数据检查命令，确认远程数据的数量和本地统计一致。
- MOMENT 首次运行需要下载预训练权重，应确认远程环境可以访问模型仓库或提前缓存权重。
- 不要让多个实验写入同一个输出目录。
- 每次训练结束后及时备份配置、指标、最佳权重、预测结果和日志。
